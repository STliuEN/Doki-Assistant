# Model Default Badge Plan

## Goal

Adjust the model settings table default marker.

## Changes

- Remove the default check mark from the left side of the model type column.
- Show a text badge `默认` near the right-side row actions.
- Keep a fixed badge placeholder width so row action buttons remain aligned.
