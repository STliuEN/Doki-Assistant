# Model Default Badge Change Log

## Frontend

- Updated `front/src/pages/ModelSettings.tsx`.
- The default model is now marked with a right-side `默认` badge.
- The model type column now only displays the model type label.
