# E2/AR-1/S1 变更日志

状态：已关闭

| 时间 | commit/文件/schema | 变更 | 原因 | 影响 | 回滚点 | 负责人 | 证据 |
|---|---|---|---|---|---|---|---|
| 2026-08-28 | `project_changes/2026-08-28-e2-ar1-sql-foundation/` | 新建 E2 `plan.md`、`change-log.md`、`test-record.md`、`schema-map.md` 和只读 SQL 盘点 | 补齐 E2 在进入 `待你确认` 前缺失的批次边界、schema ownership、合同、环境和证据占位 | 仅文档和静态盘点；未创建容器、未改 schema/代码、未连接数据库 | 删除本批新增文档；不影响运行状态 | Codex | `E2-PREP-01` 至 `E2-PREP-04`、`E2-PREP-07` |
| 2026-08-28 | `README.md`、`project_changes/README.md`、`docs/architecture_rewrite_plan.md` | 同步 E1 已关闭、E2 待确认和本批入口链接 | 根 README 仍保留 2026-08-25 的“当前停留 AR-0”旧口径；活计划尚未链接 E2 批次 | 仅文档状态入口；该记录反映授权前的准备时点，随后已取得 E2 实施授权 | 恢复对应文档行 | Codex | `E2-PREP-02`、`E2-PREP-04` |
| 2026-08-28 | `docs/stage-execution-record-template-2026-08-26.md` | 将证据状态统一为四个值，并把失败/无效/fixture 类型拆到“结果/处置” | 防止 E1 式证据状态继续漂移 | 仅后续记录格式；不重写 E1 历史事实 | 恢复模板变更 | Codex | `E2-PREP-03` |
| 2026-08-28 | 准备批次验证 | 运行现有 SQL/migration/transaction/backup 定向测试、文档检查和 diff check | 确认准备基线可重复且本批没有代码/schema 变更 | `45 passed`；Markdown `182 files, 163 local links`；`git diff --check` exit 0 | 无运行状态变更；测试均为 tmp_path/SQLite/source tree | Codex | `E2-PREP-05`、`E2-PREP-06` |
| 2026-08-28 | 用户授权与合同冻结 | Q1-Q91 grilling 完成，冻结 schema、UoW、job、runner、双归档、恢复和停线边界 | 取得 E2 实施授权并将计划状态切换为 `实施中` | 允许后续仅在批准隔离拓扑实施；真实业务资源仍 deny | 如停线则停止写入并保留现场 | Codex | 授权口令、`schema-map.md`、`plan.md` |
| 2026-08-28 | `backend/app/models/`、`backend/alembic/versions/`、`backend/app/db/db_config.py`、`backend/alembic/env.py` | 落地 canonical identity/auth/job/audit/RAG/Skill foundation、三条线性 Alembic revision、精确 head gate 和 model/migration parity 合同 | 将已批准的 E2 schema 结构化为可审阅的 SQL foundation；不触碰 populated legacy tables | 新结构仅供 E2 隔离库和合成数据；未执行 migration 或真实数据库 DDL | 回退本批对应代码/revision commit；不对数据库执行破坏性 downgrade | Codex | `E2-LOCAL-SCHEMA`、`tests/test_foundation_models.py`、`tests/test_migration_contract.py` |
| 2026-08-28 | `backend/app/db/uow.py`、`backend/app/jobs/`、`backend/main.py`、`backend/app/router/health.py` | 落地 SQL UoW、job repository/state machine、lease/heartbeat/fencing/retry/cancel/DLQ/backpressure、单并发 runner、graceful lifecycle 和独立 runner health | 按冻结合同提供 SQL-only durable runner；只注册 E2 deterministic handlers，不接业务/RAG handler | 本地 SQLite/contract tests 已通过；真实 MySQL `GET_LOCK`、`SKIP LOCKED`、kill/restart 仍未运行 | 停用 `E2_RUNNER_ENABLED` 并回退对应代码；不影响现有业务 DB | Codex | `E2-LOCAL-JOBS`、`tests/test_job_repository.py`、`tests/test_job_runner.py`、`tests/test_e2_runtime.py` |
| 2026-08-28 | `backend/scripts/backup_restore.py`、`backend/app/db/e2_guard.py` | 增加显式 preflight issuance CLI、目标/container/server UUID 校验、guarded mysqldump/import、source provenance metadata、inventory/digest 对账和未知 SQL 值 fail-closed | 防止手工 JSON、旧 bundle、漂移容器或不稳定 `repr` 进入恢复/对账路径 | 只接受明确 E2 DSN、短期 preflight 和带 source metadata 的 dump；实际 Docker/MySQL 操作仍未执行 | 回退脚本和 guard 变更；不删除任何 bundle/容器 | Codex | `E2-LOCAL-GUARD`、`tests/test_e2_guard.py`、`tests/test_backup_restore.py` |
| 2026-08-28 | `backend/tests/` | 运行 E2 scoped/full 回归、负向 guard、UoW/job/runner、备份和 migration 合同测试 | 验证本地实现没有回归并保留 fixture/mock 与 live 证据边界 | scoped `91 passed`；full `343 passed`；Ruff scoped 通过；`uv lock --check` 通过 | 无运行状态变更；测试使用 tmp_path/SQLite/模拟 guard | Codex | `E2-LOCAL-REGRESSION`、pytest/Ruff 输出 |
| 2026-08-28 | `doki-e2-20260828-mysql`、`doki-e2-20260828-mysql-restore`、`doki-e2-20260828-net` | 在批准的 E2 拓扑完成短时 preflight、空库 migration、最终 source dump 和 source/restore inventory | 获取真实 MySQL 8.4 schema/revision/约束与恢复证据，同时保持业务库和 E1 资源隔离 | 两库均为 `20260828_0005_rag_skill`、33 张表；最终 source/restore inventory SHA-256 均为 `df1bad4c3e31b51b5b7e73da3f69f129524b4192ea70549249d9dc696469071f` | 只在批准 E2 容器执行；未读取 `.env`，未访问业务库 | Codex | `artifacts/preflight/source-gate-final.json`、`artifacts/preflight/restore-gate-final.json`、`artifacts/mysql/source-inventory-gate-final.json`、`artifacts/mysql/restore-inventory-gate-final.json` |
| 2026-08-28 | E2 source SQL job rows / runner process | 完成真实 MySQL runner 矩阵：幂等重复/冲突、retry、cancel、unknown handler DLQ、backpressure、fencing、`GET_LOCK` 互斥和 kill/restart | 验证冻结的 at-least-once、fenced/idempotent commit 和单并发生命周期合同 | probe prefix `e2-live-1787911709`；63 个 job 全部终态，76 个 attempt 无 active/orphan；旧 token 被拒绝，新 token 成功；kill/restart 首进程退出码 1、第二进程退出码 0，attempt 1 abandoned、attempt 2 succeeded | synthetic fixtures 仅用于 E2 source；终态 gate 和原始 restart events 独立留存 | Codex | `artifacts/mysql/e2-live-runner-probe-execution.json`、`artifacts/mysql/source-job-terminal-final.json`、`artifacts/logs/e2-live-1787911709-restart-events.jsonl` |
| 2026-08-28 | E2 SQL dump / restore-forward | 以 source final bundle 执行受 guard 保护的 restore-forward，并与独立 restore inventory 对账 | 验证 manifest provenance、行数/content digest/约束和零差异恢复 | bundle content SHA-256 `7fa3e0888b99823cbb4650dc792e34c352d8fd9b5308ae0da331d50be3f20b9d`；restore-forward verified；source/restore inventory 零差异 | 非空 restore 目标先被安全拒绝；仅在批准 restore 库内完成受控清理后重试，现场 inventory 保留 | Codex | `artifacts/mysql/source-final-execution.bundle/manifest.json`、`artifacts/logs/restore-forward-final-execution.log`、`artifacts/mysql/restore-inventory-before-final-reset.json`、`artifacts/mysql/source-restore-inventory-compare-final.json` |
| 2026-08-28 | `backend/openapi.json`、最终门禁 | 完整 pytest/Ruff/API contract/OpenAPI/lock/docs/diff 收口，并重新生成 `/health/runner` OpenAPI 合同 | 确保实现、公开合同、锁文件和状态记录一致后再提交用户审阅 | full pytest `344 passed`；API contract `11 passed`；Ruff/OpenAPI/lock/docs/diff 全部通过；OpenAPI diff 仅新增独立 runner health route | 恢复 `openapi.json` 和相关 route 可回退公开合同；不改变数据库或容器 | Codex | `E2-12-final-static-gates`、`backend/openapi.json` |
| 2026-08-28 | Docker boundary verification | 只读核对 E1/E2 容器、network 和 volume 的最终状态 | 证明 E1 保护资源未被触碰，E2 仍严格在批准拓扑内 | E1 containers `exited`；E2 containers `running` only on `doki-e2-20260828-net` with loopback `33317/33318`; all four named volumes retained | 这是关闭前快照；关闭动作另有独立记录 | Codex | `E2-13-resource-boundary`、`artifacts/logs/resource-boundary-final.json` |
| 2026-08-28 | E2 closure / resource stop | 用户回复 `批准关闭 E2` 后停止两个 E2 MySQL 容器 | 停止 E2 写入并保留可审阅现场 | `doki-e2-20260828-mysql` 与 `doki-e2-20260828-mysql-restore` 均 `exited (0)`；E1 容器仍 `exited`；四个 volume 和两个 network 保留；未删除或清理资源 | 如需复核可由用户自行启动；本批不执行 Git 或 Docker 清理 | Codex | `E2-14-close-resource-boundary`、`artifacts/logs/resource-boundary-closure.json` |
| 2026-08-28 | E2 closure static gates | 状态文档同步后复跑全量 pytest、Ruff、OpenAPI、lock 和无 Git Markdown 检查 | 证明关闭记录没有引入代码、合同或文档漂移，同时遵守用户禁止任何 Git 操作的边界 | `344 passed, 1 warning`；Ruff/OpenAPI/lock 通过；Markdown `225 files, 171 local links` | 未运行 `scripts/check-docs.ps1` 或 `git diff --check`，因为前者内部调用 Git；版本控制检查由用户管理 | Codex | `E2-15-close-static-gates`、命令输出、`test-record.md` |

## 边界与明确未做

- E2 source/restore 容器和 network 已按批准拓扑创建并验证；未创建、启动、停止、修改或清理 E1 Docker container、volume 或 network，E1 资源仍保持原状。
- 未读取 `backend/.env`、`DjangoUserService/.env`，未连接现有 MySQL/Redis/Chroma/Storage；只连接批准的 E2 source/restore MySQL。
- 已修改本批 E2 backend 代码、SQLAlchemy model、Alembic revision、`DATABASE_SCHEMA_REVISION` 和测试；未修改现有业务 service/router 的数据行为。
- 已在批准的 E2 空库执行 `alembic upgrade head`、真实 `mysqldump`/SQL import、inventory 和 restore-forward；未对 populated 业务库执行 migration、停写或删除，也未使用破坏性 downgrade。
- 保留并单独标注历史失败 probe：初次 host-side dump 因 `[WinError 2]` 未找到客户端，初次 runner probe 因 restart fixture 未及时收束；后续 container-exec dump、修复后的 runner probe 均成功。另保留非空 restore 安全拒绝和 fixture cleanup 现场，不将失败记录覆盖为成功。
- 当前 E2 为 `已关闭`；用户于 2026-08-28 明确确认关闭；不宣称 `SKILL-GATE` 或 `ARCH-GATE` 已通过。
